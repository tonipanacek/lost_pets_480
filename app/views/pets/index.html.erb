<div class="container mt-5">
  <h1>Lost pets 🐩 🐈 🐢</h1>
  <ul>
    <% @pets.each do |pet| %>
    <li><%= link_to pet.name, pet_path(pet) %></li>
    <% end %>
  </ul>

  <%= link_to 'Add a new lost pet', new_pet_path  %>
</div>
